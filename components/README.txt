the creation menu for new projects is in "Drawer2.tsx"
set "exporting" to true in "config.tsx" when building!!!!
the svgs in "public" aren't actually used, they're only for copying their data
graph components are created in "create.tsx"