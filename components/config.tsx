const exporting = false; //set this to false when you're using localhost
//and set to true when running as html, adds .html to the files in the url
const release = false //disables all debug components
let projectName = 'NULL'

//not everything uses these variables for these colors
const orange = "#D35400"
const gray = "#3b3b3b"

export {exporting, release, orange, gray}